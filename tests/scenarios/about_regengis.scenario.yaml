version: 1
scenario_id: about_regengis_smoke
algorithm_id: regengis_toolbox:about_regengis
description: Smoke scenario for the plugin about/community action.
status: template
requires:
  qgis_runtime: true
  headless_safe_by_default: false
execution:
  mode: pyqgis_or_gui
contract: ../contracts/about_regengis.contract.yaml
result_capture:
  format: json
  path: ${OUTPUT_ROOT}/about_regengis/result.json
